# LEARNING PLAYWRIGHT BATCH

Repository for Playwright learning files and exercises.

Structure:
- `chapter_01_basics/` — example scripts.

How to run (if Node/Playwright project):

1. Install dependencies:

```powershell
npm install
```

2. Run tests with Playwright:

```powershell
npx playwright test
```
